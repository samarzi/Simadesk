# SimaDesk — Задачи для разработчика

> Текущая версия: 2.0.0 | Стек: TypeScript + Vite + Vanilla DOM + Supabase + VPS (Docker)
> Дата: 2026-07-22

---

## Выполнено

| Задача | Дата |
|--------|------|
| Секреты в git (.env исключены) | 2026-07-16 |
| Авторизация Telegram + Яндекс OAuth | 2026-07 |
| Система компаний + мультикомпании | 2026-07 |
| SQL-миграции multiuser/RLS | 2026-07 |
| CI/CD (GitHub Actions → VPS) | 2026-07 |
| Биллинг + ЮKassa + тарифы | 2026-07 |
| Telegram-уведомления (edge function) | 2026-07 |
| SEO-модуль (496 строк) | 2026-06 |
| Репрайсер (1 585 строк + подмодули) | 2026-06 |
| Аудит SKU (928 строк) | 2026-06 |
| Автоответы на отзывы (1 420 строк) | 2026-06 |
| Чаты WB/Ozon/ЯМ (1 132 строки) | 2026-06 |
| Менеджер задач (2 007 строк) | 2026-06 |
| Поставщики + закупки (6 040 строк) | 2026-05 |
| Автоматизация складов/тарифов (1 969 строк) | 2026-05 |
| Витрина SimaStore (1 269 строк) | 2026-05 |
| Документы — редактор Word + Excel (1 540 строк) | 2026-05 |
| Chrome Extension (4 600 строк) | 2026-04 |

---

## 🔴 Срочно + Важно (ДЕЛАТЬ ПРЯМО СЕЙЧАС)

### 1. Sentry (Error Tracking)

**Зачем:** Без этого не узнаёшь о багах в production. Пользователи молчат, пока всё не сломается.

**Что сделать:**
1. `npm install @sentry/browser`
2. Инициализация в `src/main.ts`:
   ```typescript
   import * as Sentry from '@sentry/browser';
   if (import.meta.env.PROD) {
     Sentry.init({
       dsn: import.meta.env.VITE_SENTRY_DSN,
       environment: 'production',
       release: '2.0.0',
       tracesSampleRate: 0.1,
     });
   }
   ```
3. Добавить `VITE_SENTRY_DSN` в `.env` на VPS и GitHub Secrets
4. Обернуть catch-блоки в `api.ts`: `Sentry.captureException(err)`

**Готово когда:** Ошибки автоматически попадают в Sentry со стектрейсом, браузером, URL, user ID.

---

### 2. Приглашение участников по ссылке

**Зачем:** Ключевая фича для multi-user. Таблица `company_invites` уже создана, но нет UI.

**Что сделать:**
1. UI: Настройки компании → вкладка «Участники» → кнопка «Пригласить»
2. Генерация invite-link с уникальным token
3. При переходе по ссылке: если авторизован — автоприсоединение к компании
4. Отображение списка участников с ролями

**Готово когда:** Владелец может пригласить участника по ссылке, участник присоединяется.

---

### 3. Unit-экономика per SKU (детальный вид)

**Зачем:** Главная ценность для продавца: «зарабатываю ли я на этом товаре?» `financeSync.ts` уже собирает данные, но нет детального вида.

**Что сделать:**
1. Расширить таблицу `cost_prices`: добавить `logistics_cost`, `packaging_cost`, `ads_cost`
2. Модуль `UnitEconomicsModule.ts`: таблица всех SKU с колонками цена → себестоимость → комиссия → логистика → маржа → прибыль
3. Красная подсветка убыточных товаров
4. Интерактивный калькулятор: «что будет если поднять цену на 10%»
5. Сводка: общая прибыль за период

**Готово когда:** На каждый товар видна чистая прибыль в рублях и маржа в %.

---

## 🟡 Важно + Не срочно (ПЛАНИРОВАТЬ НА 1-2 МЕСЯЦА)

### 4. Разбить App.ts на модули

**Зачем:** 1971 строка — невозможно работать двум разработчикам. Merge-конфликты. Ошибка в одном месте ломает всё.

**Декомпозиция:**
```
App.ts (целевой размер: 300-500 строк)
├── views/TableView.ts       — виртуализация, сортировка, drag столбцов
├── views/CardView.ts        — карточный вид
├── components/Sidebar.ts    — сайдбар, список коробок
├── components/FilterPanel.ts — фильтры
├── components/SearchBar.ts  — поиск с debounce
├── components/ImportFlow.ts — импорт xlsx
├── components/ExportFlow.ts — экспорт xlsx
├── components/TopBar.ts     — верхняя панель
└── components/ProductDetail.ts — модалка товара
```

**Готово когда:** App.ts ≤ 500 строк, два разработчика могут работать параллельно без конфликтов.

---

### 5. Тесты (60% покрытие на сервисах)

**Зачем:** Сейчас 19 тестов. При рефакторинге App.ts без тестов — регрессии.

**Приоритет тестирования:**
1. `priceFormulas.ts` — 90%+ (денежные расчёты, нулевая толерантность)
2. `api.ts`, `ozonApi.ts`, `wbApi.ts`, `yandexApi.ts` — CRUD, обработка ошибок
3. `financeSync.ts` — расчёт финансов
4. `authService.ts` — авторизация

**Готово когда:** `npm test` проходит, покрытие сервисов ≥ 60%.

---

### 6. Автобиддер WB

**Зачем:** Управление рекламными ставками. API WB Promotion уже готов.

**Стратегии:**
1. Удержание позиции (топ-N)
2. Бюджет на день (равномерное распределение)
3. По ROI (если ROI < порога → снижать)
4. Время суток (пиковые часы)

**Таблица:** `bidder_rules` в Supabase
**Cron:** каждые 30 минут
**Защита:** мин/макс ставка, дневной лимит

---

### 7. P&L дашборд

**Зачем:** Одна страница: выручка − расходы = прибыль. Данные уже есть в `financeSync.ts`.

**Структура:**
```
Выручка (Ozon + WB + ЯМ)
− Себестоимость
− Комиссии МП
− Логистика
− Реклама
− Возвраты
− Налоги
= Чистая прибыль + маржинальность
```

**Графики:** линейный, столбчатый, pie-chart
**Экспорт:** XLSX для бухгалтера

---

### 8. Массовое редактирование

**Зачем:** С 500 товарами нельзя менять цену по одному.

**Что сделать:**
- Чекбоксы в таблице (Shift+клик для диапазона)
- Панель: «Изменить цену / Скрыть / Удалить / Экспорт»
- Массовое изменение цены с превью «было → стало»
- Batch-обновление на маркетплейсах через API

---

### 9. REST API для интеграций

**Зачем:** Крупные клиенты хотят интегрировать 1С, Google Sheets, Power BI.

**Эндпоинты:**
- `GET /api/v1/products` — товары
- `GET /api/v1/orders` — заказы
- `GET /api/v1/analytics/kpi` — метрики
- `POST /api/v1/webhooks` — подписка на события

**Авторизация:** API-ключ в заголовке `X-API-Key`
**Rate limiting:** 100 запросов/мин

---

### 10. Telegram Mini App

**Зачем:** Мобильный доступ без браузера. У конкурентов нет.

**Что показывать:**
- Дашборд: заказы, выручка, остатки
- FBS-заказы для сборки
- Уведомления
- Быстрые действия

**Авторизация:** автоматически через `telegram_id`

---

## Порядок выполнения

```
Сейчас:        Sentry → Приглашения → Unit-экономика
Месяц 1-2:     Разбить App.ts → Тесты → Автобиддер
Месяц 2-3:     P&L → Массовое редактирование → API → TG Mini App
```

---

*Документ: 2026-07-22. 10 задач (3 срочных + 7 важных). Удалены 11 ненужных задач.*
